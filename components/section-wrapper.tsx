
const SectionWrapper = ({ children }) => {
    return (
        <section className="px-spacing-x pt-spacing-t flex flex-col gap-12">
            {children}
        </section>
    )
}

export default SectionWrapper
